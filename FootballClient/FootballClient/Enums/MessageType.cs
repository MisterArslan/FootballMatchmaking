namespace FootballClient.Enums
{
    public enum MessageType
    {
        CONNECT,
        CREATE_INVITE,
        RECEIVE_INVITE,
        ACCEPT_INVITE,
        DECLINE_INVITE,
        CANCEL_INVITE,
        DISCONNECT,
        ERROR
    }
}