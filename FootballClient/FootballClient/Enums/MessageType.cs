namespace FootballClient.Enums
{
    public enum MessageType
    {
        CREATE_INVITE,
        ACCEPT_INVITE,
        DECLINE_INVITE,
        CANCEL_INVITE,
        DISCONNECT,
        ERROR
    }
}